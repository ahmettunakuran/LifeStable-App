import 'package:flutter_test/flutter_test.dart';
import 'package:project_lifestable/main.dart';

void main() {
  // Default smoke test removed — app uses Firebase which requires
  // a real device or emulator to run properly
}
